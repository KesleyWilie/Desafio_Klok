# Configurações Magazine Luiza
magalu.url=https://www.magazineluiza.com.br/

# Configurações Amazon
amazon.url=https://www.amazon.com.br/

# Configurações gerais de automação
browser=chrome # Opções: chrome, firefox
timeout.seconds=15 # Timeout padrão para esperas explícitas (WebDriverWait)
headless=false # true para rodar em modo headless (sem interface gráfica), false para rodar com interface

# Configuração de timeout implícito (atualmente não utilizado ativamente no BasePage, pois usa waits explícitos)
# Se for habilitar, certifique-se de entender as implicações de misturar waits.
# implicit.wait.seconds=0